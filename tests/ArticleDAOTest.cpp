#include "catch.hpp"
#include "fakeit.hpp"
#include "../src/database/ArticleDAO.hpp"

TEST_CASE("Should return valid data for existing ID.", "[ArticleDAO]") {
    //Mock<SomeInterface> mock;
    //std::shared_ptr<im::database::Database> database = std::make_shared<im::database::MockDatabase>();
    //im::database::ArticleDAO articleDAO(database);
    //uint32_t id = 1;

    //REQUIRE(articleDAO.getById(id) != nullptr);
    REQUIRE(true == true);
}
